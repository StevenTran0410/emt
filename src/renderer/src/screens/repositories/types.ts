import type { ClonePolicy, LocalRepo, RepoSnapshot } from '../../types/electron'

export type ScreenMode = 'aeh' | 'code_analysis'

export type SyncMode = 'latest' | 'pinned'

export type { ClonePolicy, LocalRepo, RepoSnapshot }
