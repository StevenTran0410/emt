[project]
name = "codespectra-backend"
version = "0.1.0"
description = "CodeSpectra analysis engine — Python backend"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "aiosqlite>=0.20.0",
    "pydantic>=2.9.0",
    "httpx>=0.27.0",
    "antlr4-python3-runtime==4.13.2",
    "langgraph==1.2.9",
    "langgraph-checkpoint-sqlite==3.1.0",
    "haystack-ai>=2.4.0",
    "pybind11>=2.13.0",
    "tree-sitter>=0.25.0",
    "tree-sitter-python>=0.25.0",
    "tree-sitter-javascript>=0.25.0",
    "tree-sitter-typescript>=0.23.0",
    "tree-sitter-go>=0.25.0",
    "tree-sitter-java>=0.23.0",
    "tree-sitter-rust>=0.24.0",
    "tree-sitter-c>=0.24.0",
    "tree-sitter-cpp>=0.23.0",
    "rank_bm25>=0.2.2",
    "tree-sitter-ruby>=0.23.1",
    "tree-sitter-php>=0.24.1",
    "tree-sitter-c-sharp>=0.23.1",
    "tree-sitter-kotlin>=1.1.0",
    "tree-sitter-scala>=0.24.0",
    "tree-sitter-bash>=0.25.1",
    "tree-sitter-lua>=0.5.0",
    "tree-sitter-zig>=1.1.2",
    "tree-sitter-haskell>=0.23.1",
    "tree-sitter-elixir>=0.3.5",
    "tree-sitter-ocaml>=0.24.2",
    "tree-sitter-julia>=0.23.1",
    "tree-sitter-yaml>=0.7.2",
    "tree-sitter-toml>=0.7.0",
    "tree-sitter-html>=0.23.2",
    "tree-sitter-css>=0.25.0",
    "tree-sitter-json>=0.24.8",
    "tree-sitter-markdown>=0.5.1",
    "tree-sitter-groovy>=0.1.2",
    "tree-sitter-cmake>=0.7.2.post1",
    "tree-sitter-svelte>=1.0.2",
    "tree-sitter-sql>=0.3.11",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "httpx>=0.27.0",
    "ruff>=0.7.0",
]
analysis = [
    "numpy>=2.1.0",
    "networkx>=3.4.0",
    "scikit-learn>=1.5.0",
]
reranker = [
    "torch>=2.0.0",
    "transformers>=4.40.0",
    "bitsandbytes>=0.42.0",
    "huggingface_hub>=0.24.0",
]
embeddings = [
    "torch>=2.0.0",
    "sentence-transformers>=3.0.0",
    "huggingface_hub>=0.24.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["api", "domain", "infrastructure", "shared"]

[tool.ruff]
target-version = "py311"
line-length = 100
# ANTLR-generated parsers are machine-produced and never hand-edited; excluding
# them keeps the linter (and editor "problems" view) focused on real source.
extend-exclude = ["**/generated/**"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
